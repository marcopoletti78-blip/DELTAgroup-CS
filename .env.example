# Copia in .env nella root del progetto (non committare .env con chiavi reali).
# Il proxy legge solo queste variabili sul server Node.

ANTHROPIC_API_KEY=sk-ant-api03-...

# Opzionale — default 8787 (deve combaciare con vite.config.js proxy)
# PROXY_PORT=8787
